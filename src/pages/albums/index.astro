---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AlbumCard from '../../components/AlbumCard.astro';
import albums from '../../data/albums.generated.json';
---

<BaseLayout
  title="Albums — T7TFOS Photography"
  description="Browse a curated archive of premium photography albums."
>
  <section class="section">
    <div class="container">
      <h1 class="section-title">Albums</h1>
      <p class="section-subtitle">
        A curated archive of location studies, commissioned stories, and signature visual essays.
      </p>
      <div class="grid grid-3" style="margin-top: 2.5rem;">
        {albums.map((album) => (
          <AlbumCard album={album} />
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
